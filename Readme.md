# Maven Repository for BDD

This repository contains cucumber & selenium dependencies.

Please extract all contents in folder c:\maven-bdd

The resulting directory structure should be:
    
    ```yml
    C:\maven-bdd
                \repository\
                \settings.xml
    ```
